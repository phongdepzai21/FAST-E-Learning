import React, { useState, useEffect } from 'react';import { sendOtpViaEmailJS } from '../utils/emailService';interface PurchaseModalProps {  isOpen: boolean;  onClose: () => void;  onSuccess: () => void;  userEmail: string;  userName: string;  courseName: string;}const PurchaseModal: React.FC<PurchaseModalProps> = ({  isOpen,  onClose,  onSuccess,  userEmail,  userName,  courseName,}) => {  const [otpSent, setOtpSent] = useState(false);  const [otpCode, setOtpCode] = useState('');  const [generatedOtp, setGeneratedOtp] = useState<string | null>(null);  const [isSending, setIsSending] = useState(false);  const [error, setError] = useState('');  useEffect(() => {    if (isOpen) {      setOtpSent(false);      setOtpCode('');      setGeneratedOtp(null);      setError('');      setIsSending(false);    }  }, [isOpen]);  const handleSendOtp = async () => {    setIsSending(true);    setError('');    const newOtp = Math.floor(100000 + Math.random() * 900000).toString();    setGeneratedOtp(newOtp);    const result = await sendOtpViaEmailJS(userEmail, userName || 'Học viên', newOtp);    if (result.success) {      setOtpSent(true);    } else {      if (result.reason === 'MISSING_KEYS') {        console.warn("EmailJS keys missing, bypassing OTP for development");        onSuccess();      } else {        setError(result.error || 'Lỗi gửi mã OTP. Vui lòng thử lại sau.');      }    }    setIsSending(false);  };  const handleVerify = () => {    if (otpCode === generatedOtp) {      onSuccess();    } else {      setError('Mã OTP không chính xác.');    }  };  if (!isOpen) return null;  return (    <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/60 backdrop-blur-sm px-4">      <div className="bg-white rounded-3xl w-full max-w-md p-6 sm:p-8 shadow-2xl relative overflow-hidden">        <button          onClick={onClose}          className="absolute top-4 right-4 text-gray-400 hover:text-gray-600 transition-colors"        >          <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />          </svg>        </button>        <div className="text-center mb-6">          <div className="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">            <svg className="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />            </svg>          </div>          <h2 className="text-2xl font-bold text-gray-900 mb-2">Xác minh danh tính</h2>          <p className="text-gray-600 text-sm">            Bạn đang đăng ký gói: <br />            <span className="font-semibold text-gray-800">{courseName}</span>          </p>        </div>        {error && (          <div className="mb-4 p-3 bg-red-50 border border-red-200 text-red-600 text-sm rounded-xl text-center">            {error}          </div>        )}        {!otpSent ? (          <div className="space-y-4">            <p className="text-sm text-gray-600 text-center">              Chúng tôi sẽ gửi một mã xác minh gồm 6 chữ số đến email <b className="text-gray-800">{userEmail}</b> của bạn để đảm bảo an toàn.            </p>            <button              onClick={handleSendOtp}              disabled={isSending}              className="w-full py-3.5 px-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-2xl transition-all shadow-lg shadow-blue-200 disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2"            >              {isSending ? (                <>                  <svg className="animate-spin h-5 w-5 text-white" fill="none" viewBox="0 0 24 24">                    <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>                    <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>                  </svg>                  Đang gửi OTP...                </>              ) : (                'Gửi mã OTP'              )}            </button>          </div>        ) : (          <div className="space-y-5">            <div>              <label className="block text-sm font-medium text-gray-700 mb-1 text-center">                Nhập mã OTP (6 số)              </label>              <input                type="text"                maxLength={6}                value={otpCode}                onChange={(e) => setOtpCode(e.target.value.replace(/\D/g, ''))}                placeholder="000000"                className="w-full text-center text-3xl font-bold tracking-[0.5em] py-3 border-2 border-gray-200 rounded-2xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 transition-all outline-none"              />            </div>                        <button              onClick={handleVerify}              disabled={otpCode.length !== 6}              className="w-full py-3.5 px-4 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-2xl transition-all shadow-lg shadow-green-200 disabled:opacity-50 disabled:cursor-not-allowed"            >              Xác nhận & Tiếp tục            </button>            <button              onClick={handleSendOtp}              disabled={isSending}              className="w-full py-2 text-sm font-medium text-blue-600 hover:text-blue-700 transition-colors"            >              Gửi lại mã            </button>          </div>        )}      </div>    </div>  );};export default PurchaseModal;
